# Test generated by RoostGPT for test go-http-server-test using AI Type Open AI and AI Model gpt-4

config:
  target: "http://localhost:8080"
  phases:
    - duration: 60
      arrivalRate: 20
  defaults:
    headers:
      Content-Type: "application/json"
scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/nonce"
          capture:
            - json: "$.nonce"
              as: "nonce"
          expect:
            - statusCode: 200
            - hasProperty: "nonce"
  - name: "API request with server error"
    flow:
      - get:
          url: "/nonce"
          beforeRequest:
            # Here we simulate a server error by messing up the database connection or something similar
            js: |
              function(context, ee, next) {
                // Code to simulate server error goes here
                next();
              }
          expect:
            - statusCode: 500
  - name: "API request with edge case"
    flow:
      - get:
          url: "/nonce"
          beforeRequest:
            # Here we simulate an edge case by sending a large number of requests at once
            js: |
              function(context, ee, next) {
                // Code to simulate edge case goes here
                next();
              }
          expect:
            - statusCode: function(status) { return status >= 200 && status < 300; } # Expect any 2xx status code
